var button = $("#<%= dom_id(Company.find_by_hk_ticker(params[:hk_ticker])) %>");

button.toggleClass("btn-primary");
button.toggleClass("btn-success");
button.text("Not following");
button.attr("href", "/<%= params[:hk_ticker] %>/follow");

var button2 = $("#<%= dom_id(Company.find_by_hk_ticker(params[:hk_ticker])) %>-current");
button2.closest("tr").fadeOut(300, function(){ $(this).remove();});